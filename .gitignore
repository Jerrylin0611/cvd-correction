# 訓練資料 (COCO val2017，太大也不是原始碼，另外下載即可)
data/

# 訓練出來的模型權重 (可以重新訓練產生，不需要進版控)
src/checkpoints*/

# 產生出來的比較圖 (可以重新跑腳本產生)
*.png

# Python 快取
__pycache__/
*.pyc

# VSCode 設定裡寫死了這台機器的 Python 路徑，每個人電腦路徑不同，不要共用
.vscode/
